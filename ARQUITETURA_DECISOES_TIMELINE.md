# Linha do tempo de decisões

1. v1 (planejada): EC2 + RDS — descartada por risco de custo
   "always-on" sem monitoramento adequado.
2. v2 (implementada): Lambda + S3 — 100% serverless, custo real $0.
3. Classificação: planejada via Anthropic API, substituída por
   correspondência de palavras-chave por restrição de orçamento —
   interface mantida idêntica para permitir troca futura.